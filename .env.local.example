# Your Anam API key from lab.anam.ai
# This is used ONLY on the server (in /api/anam-token) to exchange for short-lived session tokens.
# Never expose this client-side.
ANAM_API_KEY=your_anam_api_key_here
